import {useSearchVideosQuery} from "../../store/slices/apiSlice.js";

function Results(){
    //const { data, isError, isLoading } = useSearchVideosQuery('lietuva')

    return (
        <>
            <h1> Results </h1>
        </>
    )
}

export default Results